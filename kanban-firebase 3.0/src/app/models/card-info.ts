export class Card {
    //tal vez deva cambiar el $, creo que es lo que da problemas.
    $key?: string;
    name?: string;
    description?: string;
    cardListId?: string;
    isExpanded?: boolean;
    order?: number;
    created_at?: string;
}
